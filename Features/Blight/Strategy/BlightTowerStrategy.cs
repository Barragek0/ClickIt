namespace ClickIt.Features.Blight.Strategy;

internal enum BlightTowerStrategy
{
    ChillingSeismicMeteor = 0,
    // Slot 1 was MeteorOnly (removed) — kept free so persisted setting values stay stable.
    LockdownArc = 2,
    EmpoweredScouts = 3
}
